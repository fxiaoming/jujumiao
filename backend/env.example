# MongoDB 配置
# 方式1: 使用完整的连接字符串
MONGO_URI=mongodb://username:password@localhost:27017/jujumiao?authSource=admin

# 方式2: 使用单独的环境变量（需要在 config.yaml 中使用 ${MONGO_USER} 等占位符）
# MONGO_USER=your_username
# MONGO_PASS=your_password
# MONGO_HOST=localhost
# MONGO_PORT=27017
# MONGO_DB=jujumiao

# Redis 配置
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# 其他配置
NODE_ENV=production 